Call save_and_compile_cv with the complete LaTeX document. If this endpoint
cannot emit a function call, return the complete document directly, beginning
with \begin{document} and ending with \end{document}. Do not use Markdown
fences or add commentary.
